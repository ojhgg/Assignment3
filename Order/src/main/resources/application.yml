
spring:
  application:
    name: ORDER-SERVICE
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/shipping?useUnicode=true&characterEncoding=UTF-8&allowMultiQueries=true;
    driverClassName: com.mysql.cj.jdbc.Driver
    username: root
    password: jinyuan
server:
  port: 8081
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    hostname: localhost
    prefer-ip-address: true